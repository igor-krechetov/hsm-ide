- New code must match existing style.
- Use a single return point per function.
- Follow this class structure:
    class <name> {
        public types
        private types
        public methods
        protected methods
        private methods
        private variables
    };